{
  "name": "blog",
  "version": "1.0.0",
  "author": "reco_luan",
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider; vuepress dev . --open --host \"localhost\"",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider; vuepress build ."
  },
  "devDependencies": {
    "vuepress": "1.9.7",
    "vuepress-theme-reco": "1.6.16"
  },
  "description": "a blog of xus"
}